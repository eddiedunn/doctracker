# Override global .gitignore for .claude directory
# This repo's slash commands should be tracked
!.claude/
!.claude/**

# Doctrack state files (regeneratable)
.doctrack/state.yaml
.doctrack/repos/*.yaml

# Note: .doctrack/session.yaml is NOT ignored
# It contains project configuration and can be committed
*tasks.yaml
*.pyc
__pycache__/
.DS_Store
